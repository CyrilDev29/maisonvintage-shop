{% extends 'base.html.twig' %}
{% block title %}Mot de passe oublié{% endblock %}

{% block body %}
    <section class="container" style="max-width:560px;margin:2rem auto;padding:0 1rem;">
        <h1 class="h4 mb-3">Mot de passe oublié</h1>
        <p class="text-muted">Entrez votre e-mail. Si un compte existe, vous recevrez un lien pour réinitialiser votre mot de passe.</p>

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

        <form method="post" action="{{ path('app_forgot_password_request') }}">
            <input type="hidden" name="_token" value="{{ csrf_token('forgot_password_request') }}">
            <label for="email" class="form-label">Votre e-mail</label>
            <input id="email" name="email" type="email" required autocomplete="email" class="form-control" value="">
            <button class="btn btn-primary mt-3" type="submit">Envoyer le lien</button>
        </form>


        <p class="mt-3">
            <a href="{{ path('app_login') }}">Retour à la connexion</a>
        </p>
    </section>
{% endblock %}

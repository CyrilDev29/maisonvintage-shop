{% extends 'base.html.twig' %}
{% block title %}E-mail envoyé{% endblock %}

{% block body %}
    <section class="container" style="max-width:560px;margin:2rem auto;padding:0 1rem;">
        <h1 class="h4 mb-3">Vérifiez votre boîte mail</h1>
        <p>Si un compte existe pour cette adresse, un e-mail contenant un lien de réinitialisation a été envoyé.</p>
        {% if tokenLifetime %}
            <p class="text-muted">Le lien expirera dans environ {{ tokenLifetime }} secondes.</p>
        {% endif %}
        <p class="text-muted">Pensez à vérifier vos spams.</p>

        <p class="mt-3">
            <a class="btn btn-outline-secondary" href="{{ path('app_forgot_password_request') }}">Renvoyer un e-mail</a>
            <a class="btn btn-link" href="{{ path('app_login') }}">Retour à la connexion</a>
        </p>
    </section>
{% endblock %}

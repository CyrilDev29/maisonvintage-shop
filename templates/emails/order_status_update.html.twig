{% extends 'base.html.twig' %}
{% block title %}Mise à jour de votre commande{% endblock %}
{% block body %}
    <section style="max-width:650px;margin:2rem auto;padding:1rem;">
        <h1>Commande {{ order.reference }}</h1>
        <p>Bonjour {{ order.prenom }} {{ order.nom }},</p>
        <p>Le statut de votre commande a changé&nbsp;: <strong>{{ order.status.value }}</strong>.</p>

        <h2 class="h5 mt-4">Récapitulatif</h2>
        <ul>
            {% for it in order.items %}
                <li>{{ it.productName }} — {{ it.quantity }} × {{ it.unitPrice|number_format(2, ',', ' ') }} €</li>
            {% endfor %}
        </ul>
        <p><strong>Total :</strong> {{ order.total|number_format(2, ',', ' ') }} €</p>

        <p class="mt-4">Vous pouvez consulter votre commande ici : <a href="{{ url('app_account_order_show', { id: order.id }) }}">détail de la commande</a>.</p>

        <p class="mt-4">Merci de votre confiance,<br>L’équipe MaisonVintage</p>
    </section>
{% endblock %}

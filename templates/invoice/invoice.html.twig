{# templates/invoice/invoice.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Facture {{ order.reference }}</title>
    <style>
        @page { margin: 40px 50px; }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: Arial, Helvetica, sans-serif;
            font-size: 11px;
            color: #000;
            line-height: 1.4;
        }

        /* Header */
        .header {
            display: table;
            width: 100%;
            padding-top: 10px;
            padding-bottom: 15px;
        }
        .header-left {
            display: table-cell;
            width: 50%;
            vertical-align: middle;
            padding-left: 40px;
        }
        .header-right {
            display: table-cell;
            width: 50%;
            text-align: right;
            vertical-align: middle;
        }
        .logo {
            max-width: 140px;
            height: auto;
        }
        .invoice-number {
            border: 2px solid #000;
            padding: 8px 15px;
            display: inline-block;
            font-size: 13px;
            font-weight: bold;
        }

        /* Date line */
        .date-line {
            border-top: 1px solid #000;
            border-bottom: 1px solid #000;
            padding: 8px 0;
            margin-bottom: 20px;
            font-size: 11px;
        }

        /* Addresses */
        .addresses {
            display: table;
            width: 100%;
            margin-bottom: 25px;
        }
        .address-box {
            display: table-cell;
            width: 48%;
            border: 1px solid #000;
            padding: 12px;
            vertical-align: top;
        }
        .address-box:first-child {
            margin-right: 4%;
        }
        .address-box h3 {
            font-size: 12px;
            font-weight: bold;
            margin-bottom: 8px;
        }
        .address-box p {
            font-size: 11px;
            line-height: 1.5;
        }

        /* Reference line */
        .reference-line {
            margin-bottom: 15px;
            font-size: 11px;
            font-weight: bold;
        }

        /* Table */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px;
        }
        thead {
            background: #fff;
            border-bottom: 1px solid #000;
        }
        th {
            padding: 8px;
            text-align: left;
            font-size: 11px;
            font-weight: bold;
            border: 1px solid #000;
        }
        th.center {
            text-align: center;
        }
        th.right {
            text-align: right;
        }
        td {
            padding: 8px;
            font-size: 11px;
            border-left: 1px solid #000;
            border-right: 1px solid #000;
        }
        td.center {
            text-align: center;
        }
        td.right {
            text-align: right;
        }
        tbody tr:last-child td {
            border-bottom: 1px solid #000;
        }

        /* Notes */
        .notes {
            font-size: 10px;
            margin-bottom: 15px;
            line-height: 1.4;
        }

        /* Totals */
        .totals-table {
            width: 350px;
            float: right;
            border-collapse: collapse;
        }
        .totals-table td {
            border: 1px solid #000;
            padding: 6px 10px;
            font-size: 11px;
        }
        .totals-table td.label {
            font-weight: bold;
            text-align: left;
            background: #fff;
        }
        .totals-table td.value {
            text-align: right;
            font-weight: bold;
        }
        .totals-table tr.final td {
            background: #fff;
            font-size: 12px;
        }

        /* Clear float */
        .clear {
            clear: both;
        }

        /* Shipping info */
        .shipping-info {
            margin-top: 20px;
            border: 1px solid #000;
            padding: 12px;
            background: #f9f9f9;
        }
        .shipping-info h3 {
            font-size: 12px;
            font-weight: bold;
            margin-bottom: 6px;
        }
        .shipping-info p {
            font-size: 11px;
            line-height: 1.5;
        }

        /* Footer */
        footer {
            margin-top: 30px;
            padding-top: 15px;
            border-top: 1px solid #000;
            font-size: 9px;
            line-height: 1.5;
        }

        /* Ribbon for seller copy */
        .ribbon {
            position: fixed;
            top: 50px;
            right: -70px;
            transform: rotate(45deg);
            padding: 8px 80px;
            background: #f0f0f0;
            border: 1px solid #000;
            color: #000;
            text-transform: uppercase;
            font-size: 10px;
            font-weight: bold;
        }
    </style>
</head>
<body>

{% if isSellerCopy %}
    <div class="ribbon">Copie vendeur</div>
{% endif %}

<!-- Header -->
<div class="header">
    <div class="header-left">
        <img src="{{ absolute_url(asset('images/logo.png')) }}" alt="Maison Vintage" class="logo">
    </div>
    <div class="header-right">
        <div class="invoice-number">Facture n° {{ order.reference }}</div>
    </div>
</div>

<!-- Date line -->
<div class="date-line">
    Date : {{ order.createdAt|date('d/m/Y') }}
</div>

<!-- Addresses -->
<div class="addresses">
    <!-- Billing address -->
    <div class="address-box">
        <h3>Adresse de facturation</h3>
        {% set bill = order.billingSnapshot ?? order.shippingSnapshot ?? null %}
        {% if bill %}
            <p>
                {{ bill.fullName }}<br>
                {{ bill.line1 }}<br>
                {% if bill.line2 %}{{ bill.line2 }}<br>{% endif %}
                {{ bill.postalCode }} {{ bill.city }}<br>
                {% if bill.phone %}{{ bill.phone }}{% endif %}
            </p>
        {% else %}
            <p>
                {{ order.prenom }} {{ order.nom }}<br>
                {{ order.rue }}<br>
                {{ order.codePostal }} {{ order.ville }}
            </p>
        {% endif %}
    </div>

    <!-- Shipping address -->
    <div class="address-box">
        <h3>Adresse de livraison</h3>
        {% set ship = order.shippingSnapshot %}
        {% if ship %}
            <p>
                {{ ship.fullName }}<br>
                {{ ship.line1 }}<br>
                {% if ship.line2 %}{{ ship.line2 }}<br>{% endif %}
                {{ ship.postalCode }} {{ ship.city }}<br>
                {% if ship.phone %}{{ ship.phone }}{% endif %}
            </p>
        {% else %}
            <p>
                {{ order.prenom }} {{ order.nom }}<br>
                {{ order.rue }}<br>
                {{ order.codePostal }} {{ order.ville }}
            </p>
        {% endif %}
    </div>
</div>

<!-- Reference -->
<div class="reference-line">
    Référence : {{ order.reference }}
</div>

<!-- Items table -->
<table>
    <thead>
    <tr>
        <th>Désignation</th>
        <th class="center">Qte</th>
        <th class="center">TVA</th>
        <th class="right">Prix unitaire</th>
        <th class="right">Prix Total</th>
    </tr>
    </thead>
    <tbody>
    {% for line in order.items %}
        {% set unit = (line.unitPrice|replace({',':'.'})) + 0 %}
        {% set total = (line.getLineTotal()|replace({',':'.'})) + 0 %}
        <tr>
            <td>{{ line.productName }}</td>
            <td class="center">{{ line.quantity }}</td>
            <td class="center">-</td>
            <td class="right">{{ unit|number_format(2, ',', ' ') }}</td>
            <td class="right">{{ total|number_format(2, ',', ' ') }} €</td>
        </tr>
    {% endfor %}

    <!-- Shipping line if applicable -->
    {% set total_port = (order.shippingAmountCents is defined ? (order.shippingAmountCents / 100) : 0) + 0 %}
    {% if total_port > 0 %}
        <tr>
            <td>Frais de port</td>
            <td class="center">1</td>
            <td class="center">-</td>
            <td class="right">{{ total_port|number_format(2, ',', ' ') }}</td>
            <td class="right">{{ total_port|number_format(2, ',', ' ') }} €</td>
        </tr>
    {% endif %}
    </tbody>
</table>

<!-- Notes -->
<div class="notes">
    * TVA non applicable, art. 293 B du CGI (Franchise en base de TVA)
</div>

<!-- Totals -->
{% set total_articles = (order.total|default(0)) + 0 %}
{% set total_ttc = total_articles + total_port %}

<table class="totals-table">
    <tr>
        <td class="label">Total HT</td>
        <td class="value">{{ total_ttc|number_format(2, ',', ' ') }} €</td>
    </tr>
    <tr>
        <td class="label">Total TVA</td>
        <td class="value">0,00 €</td>
    </tr>
    <tr class="final">
        <td class="label">Total TTC</td>
        <td class="value">{{ total_ttc|number_format(2, ',', ' ') }} €</td>
    </tr>
</table>

<div class="clear"></div>

<!-- Shipping info if different carriers -->
{% if order.shippingCarrier or order.shippingMethod %}
    <div class="shipping-info">
        <h3>Informations de transport</h3>
        <p>
            {% if order.shippingCarrier %}<strong>Transporteur :</strong> {{ order.shippingCarrier }}{% endif %}
            {% if order.shippingMethod %} - {{ order.shippingMethod }}{% endif %}
            {% if order.shippingRelayId %}<br><strong>Point relais :</strong> {{ order.shippingRelayId }}{% endif %}
        </p>
    </div>
{% endif %}

<!-- Footer -->
<footer>
    <p><strong>Conditions de paiement :</strong> Paiement comptant</p>
    <p><strong>Pénalités de retard :</strong> Taux légal en vigueur (3 fois le taux d'intérêt légal)</p>
    <p><strong>Indemnité forfaitaire pour frais de recouvrement :</strong> 40 euros</p>
    <p><strong>Escompte :</strong> Aucun escompte pour paiement anticipé</p>
    <p style="margin-top: 8px;">
        <strong>Maison Vintage - EI</strong> - 95 Kerizac, 29870 Landéda<br>
        SIRET 752 102 897 00036 - TVA FR45752102897 - RCS Brest 752 102 897<br>
        Facture émise le {{ 'now'|date('d/m/Y à H:i') }}
    </p>
</footer>

</body>
</html>
